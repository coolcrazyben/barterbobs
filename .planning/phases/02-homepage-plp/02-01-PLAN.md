---
phase: 02-homepage-plp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/index.json
  - sections/section-hero.liquid
  - sections/section-curation-pillars.liquid
  - sections/section-category-browse.liquid
  - sections/section-featured-products.liquid
  - sections/section-brand-story.liquid
  - sections/section-how-it-works.liquid
  - sections/section-testimonials.liquid
  - sections/section-cta-banner.liquid
  - snippets/product-card.liquid
  - assets/component-hero.css
  - assets/component-curation-pillars.css
  - assets/component-category-browse.css
  - assets/component-featured-products.css
  - assets/component-brand-story.css
  - assets/component-how-it-works.css
  - assets/component-testimonials.css
  - assets/component-cta-banner.css
  - assets/component-product-card.css
autonomous: false

requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "The homepage renders all 8 sections in Figma order: Hero, Curation Pillars, Category Browse, Featured Products, Brand Story, How It Works, Testimonials, CTA Banner"
    - "Hero image uses loading='eager' and no LCP penalty is reported"
    - "Featured Products grid pulls from a merchant-selected collection via the Shopify Theme Editor"
    - "How It Works renders 3 numbered gradient-circle steps, all configurable in Theme Editor"
    - "Testimonials renders multiple quote cards with stars, name, and location — blocks addable via Theme Editor"
    - "shopify theme check passes with zero errors and zero warnings after all sections are added"
  artifacts:
    - path: "sections/section-hero.liquid"
      provides: "Hero section with display headline, subheadline, CTA buttons, hero image, Theme Editor schema"
    - path: "sections/section-curation-pillars.liquid"
      provides: "Curation Pillars section with 3 icon+headline+body repeatable blocks"
    - path: "sections/section-category-browse.liquid"
      provides: "Category Browse section with staggered 4-card portrait grid, repeatable blocks"
    - path: "sections/section-featured-products.liquid"
      provides: "Featured Products 4-column grid sourcing from merchant-selected collection"
    - path: "sections/section-brand-story.liquid"
      provides: "Brand Story 2-col editorial section with headline, body, 2x2 image grid"
    - path: "sections/section-how-it-works.liquid"
      provides: "How It Works 3-step flow with numbered gradient circles"
    - path: "sections/section-testimonials.liquid"
      provides: "Testimonials section with star-rated quote cards"
    - path: "sections/section-cta-banner.liquid"
      provides: "CTA Banner full-width rounded red card with headline and CTA button"
    - path: "snippets/product-card.liquid"
      provides: "Shared product card snippet used by homepage featured grid and PLP"
    - path: "templates/index.json"
      provides: "Homepage template referencing all 8 sections in order"
  key_links:
    - from: "templates/index.json"
      to: "sections/section-hero.liquid (and all 8 sections)"
      via: "JSON section references matching section file names"
      pattern: "\"type\": \"section-hero\""
    - from: "sections/section-featured-products.liquid"
      to: "snippets/product-card.liquid"
      via: "{% render 'product-card', product: product %}"
      pattern: "render 'product-card'"
    - from: "sections/section-hero.liquid"
      to: "assets/component-hero.css"
      via: "{{ 'component-hero.css' | asset_url | stylesheet_tag }}"
      pattern: "component-hero.css.*stylesheet_tag"
---

<objective>
Implement all 8 homepage sections and the shared product card snippet.

Purpose: Delivers the complete browsable homepage — the editorial storefront surface a shopper lands on. Every section is merchant-configurable via Theme Editor and matches the barterbobs_main_landing_page Figma export.

Output: 8 Liquid section files, 8 component CSS files, 1 shared product-card snippet, updated index.json. shopify theme check passes zero errors/warnings.
</objective>

<execution_context>
@C:/Users/pfpee/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pfpee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key patterns from Phase 1 that all sections MUST follow. -->

From assets/global.js (window.BarterBobs namespace):
```js
window.BarterBobs.publish(eventName, data)   // pubsub dispatch
window.BarterBobs.subscribe(eventName, handler) // returns unsubscribe fn
window.BarterBobs.trapFocus(container, elementToFocus) // returns cleanup fn
window.BarterBobs.ShopifySection  // base class — extend for interactive sections
```

Section file header pattern (REQUIRED — avoids LiquidHTMLSyntaxError):
```liquid
{%- comment -%}
  sections/section-hero.liquid
  [One-line description]
{%- endcomment -%}

{{ 'component-hero.css' | asset_url | stylesheet_tag }}
```

Section-specific JS pattern (inline in liquid, NOT a separate .js asset file):
```liquid
<script type="module">
  class SectionHero extends window.BarterBobs.ShopifySection {
    onSectionLoad() { /* init */ }
    onSectionUnload() { /* cleanup document listeners only */ }
  }
  customElements.define('section-hero', SectionHero);
</script>
```

Design tokens available from snippets/css-variables.liquid:
```css
--color-brand-primary    /* red CTA color */
--color-surface          /* page background */
--color-text             /* primary text */
--color-text-secondary   /* muted text */
--font-display           /* Newsreader serif */
--font-body              /* Inter */
--transition-base        /* 200ms ease — use for ALL hover states */
--section-gap            /* 12rem — between sections */
--section-gap-sm         /* 6rem — compact variant */
```

CartCountBubble event (Quick Add must use):
```js
window.BarterBobs.publish('cart:updated', { itemCount: N });
window.BarterBobs.publish('cart:open', {});   // Phase 3 drawer will subscribe
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section stubs, CSS files, product card snippet, and index.json update</name>
  <files>
    templates/index.json,
    sections/section-hero.liquid,
    sections/section-curation-pillars.liquid,
    sections/section-category-browse.liquid,
    sections/section-featured-products.liquid,
    sections/section-brand-story.liquid,
    sections/section-how-it-works.liquid,
    sections/section-testimonials.liquid,
    sections/section-cta-banner.liquid,
    snippets/product-card.liquid,
    assets/component-hero.css,
    assets/component-curation-pillars.css,
    assets/component-category-browse.css,
    assets/component-featured-products.css,
    assets/component-brand-story.css,
    assets/component-how-it-works.css,
    assets/component-testimonials.css,
    assets/component-cta-banner.css,
    assets/component-product-card.css
  </files>
  <action>
    Create the full scaffold of files that makes shopify theme check pass before implementing content.

    **templates/index.json** — Replace existing `main-page-content` reference with all 8 homepage sections in Figma order:
    ```json
    {
      "sections": {
        "hero":               { "type": "section-hero",              "settings": {} },
        "curation-pillars":   { "type": "section-curation-pillars",  "settings": {},
                                "blocks": { "pillar-1": {"type":"pillar","settings":{}}, "pillar-2": {"type":"pillar","settings":{}}, "pillar-3": {"type":"pillar","settings":{}} },
                                "blocks_order": ["pillar-1","pillar-2","pillar-3"] },
        "category-browse":    { "type": "section-category-browse",   "settings": {},
                                "blocks": { "card-1": {"type":"card","settings":{}}, "card-2": {"type":"card","settings":{}}, "card-3": {"type":"card","settings":{}}, "card-4": {"type":"card","settings":{}} },
                                "blocks_order": ["card-1","card-2","card-3","card-4"] },
        "featured-products":  { "type": "section-featured-products", "settings": {} },
        "brand-story":        { "type": "section-brand-story",       "settings": {} },
        "how-it-works":       { "type": "section-how-it-works",      "settings": {},
                                "blocks": { "step-1": {"type":"step","settings":{}}, "step-2": {"type":"step","settings":{}}, "step-3": {"type":"step","settings":{}} },
                                "blocks_order": ["step-1","step-2","step-3"] },
        "testimonials":       { "type": "section-testimonials",      "settings": {},
                                "blocks": { "quote-1": {"type":"testimonial","settings":{}}, "quote-2": {"type":"testimonial","settings":{}} },
                                "blocks_order": ["quote-1","quote-2"] },
        "cta-banner":         { "type": "section-cta-banner",        "settings": {} }
      },
      "order": ["hero","curation-pillars","category-browse","featured-products","brand-story","how-it-works","testimonials","cta-banner"]
    }
    ```

    **Each section liquid stub** — Create with correct comment header, CSS asset tag, minimal valid HTML placeholder, and a complete Theme Editor `{% schema %}` block. The schema defines all settings and block types the full implementation will need. This pattern ensures theme check passes before content HTML is written.

    Example stub structure (apply to all 8 sections):
    ```liquid
    {%- comment -%}
      sections/section-hero.liquid
      Hero section — display headline, subheadline, CTA buttons, hero image
    {%- endcomment -%}

    {{ 'component-hero.css' | asset_url | stylesheet_tag }}

    <section class="section-hero" data-section-id="{{ section.id }}">
      <!-- Implementation in Task 2 -->
    </section>

    {% schema %}
    {
      "name": "Hero",
      "tag": "section",
      "class": "section-hero-wrapper",
      "settings": [
        { "type": "text", "id": "headline", "label": "Headline", "default": "Artisan provisions, delivered with care." },
        ...
      ]
    }
    {% endschema %}
    ```

    **Schema definitions for each section:**

    `section-hero`: settings — headline (text), subheadline (textarea), cta_primary_label (text), cta_primary_url (url), cta_secondary_label (text), cta_secondary_url (url), hero_image (image_picker). No blocks.

    `section-curation-pillars`: settings — headline (text, "Curation with Care"). Block type `pillar`: icon (text, Material Symbol name), pillar_headline (text), pillar_body (textarea). max_blocks: 3.

    `section-category-browse`: settings — headline (text, "Browse Our Pantry"). Block type `card`: image (image_picker), label (text), link (url). max_blocks: 4.

    `section-featured-products`: settings — headline (text, "This Month's Favorites"), subheadline (text), collection (collection). No blocks. (Uses `section.settings.collection` to source products.)

    `section-brand-story`: settings — headline (text), body (richtext), pull_quote (text), image_1 through image_4 (image_picker x4). No blocks.

    `section-how-it-works`: settings — headline (text, "How It Works"). Block type `step`: step_number (text), step_headline (text), step_body (textarea). max_blocks: 3.

    `section-testimonials`: settings — headline (text, "Loved by Bob-scribers"). Block type `testimonial`: quote (textarea), customer_name (text), location (text), rating (range 1-5 step 1 default 5), avatar (image_picker). max_blocks: 12.

    `section-cta-banner`: settings — headline (text, "Ready to elevate your pantry?"), offer_copy (text, "Get $20 off your first two boxes"), cta_label (text, "Claim Your Offer"), cta_url (url). No blocks.

    **snippets/product-card.liquid** — Create the shared product card snippet. Accepts `product` object. Renders: product image (aspect-ratio 1/1, class "product-card__image", hover scale via CSS), badge label (from product type — render if present), product title, price, Quick Add button. Quick Add button: `<button class="product-card__quick-add" data-product-id="{{ product.id }}" data-variants-count="{{ product.variants | size }}" aria-label="Quick add {{ product.title }}">Quick Add</button>`. The button carries data attributes; section JS handles the click event (not inline onclick). No JS in this snippet.

    ```liquid
    {%- comment -%}
      snippets/product-card.liquid
      Shared product card — used by section-featured-products and main-collection
      Required parameters: product (product object)
    {%- endcomment -%}

    <article class="product-card">
      <a href="{{ product.url }}" class="product-card__image-link" tabindex="-1" aria-hidden="true">
        <div class="product-card__image-wrapper">
          {%- if product.featured_image -%}
            {{ product.featured_image | image_url: width: 600 | image_tag:
              class: 'product-card__image',
              alt: product.featured_image.alt | escape,
              loading: 'lazy',
              width: 600,
              height: 600
            }}
          {%- else -%}
            {{ 'product-1' | placeholder_svg_tag: 'product-card__image product-card__image--placeholder' }}
          {%- endif -%}
        </div>
      </a>

      {%- if product.type != blank -%}
        <span class="product-card__badge">{{ product.type }}</span>
      {%- endif -%}

      <div class="product-card__info">
        <h3 class="product-card__title">
          <a href="{{ product.url }}">{{ product.title }}</a>
        </h3>
        <p class="product-card__price">{{ product.price | money }}</p>
      </div>

      <button
        class="product-card__quick-add"
        data-product-id="{{ product.id }}"
        data-variants-count="{{ product.variants | size }}"
        aria-label="{{ 'products.quick_add' | t: title: product.title | escape }}"
      >
        {{- 'products.quick_add_label' | t | default: 'Quick Add' -}}
      </button>
    </article>
    ```

    Add translation keys to `locales/en.default.json`:
    ```json
    "products": {
      "quick_add": "Quick add {{ title }}",
      "quick_add_label": "Quick Add"
    }
    ```

    **All 9 CSS files** — Create with a minimal comment header only (full styles written in Task 2). This ensures `stylesheet_tag` references in section stubs do not cause "asset not found" theme check errors.

    ```css
    /* assets/component-hero.css — Hero section styles */
    /* Full implementation in plan 02-01, Task 2 */
    ```

    After creating all files, run `shopify theme check` and fix any errors before committing.
  </action>
  <verify>
    <automated>shopify theme check</automated>
  </verify>
  <done>shopify theme check passes zero errors and zero warnings. templates/index.json references all 8 section types. All 8 section liquid stubs and 9 CSS stub files exist. snippets/product-card.liquid exists with Quick Add button data attributes. locales/en.default.json has products.quick_add and products.quick_add_label keys.</done>
</task>

<task type="auto">
  <name>Task 2: Implement all 8 homepage sections and CSS</name>
  <files>
    sections/section-hero.liquid,
    sections/section-curation-pillars.liquid,
    sections/section-category-browse.liquid,
    sections/section-featured-products.liquid,
    sections/section-brand-story.liquid,
    sections/section-how-it-works.liquid,
    sections/section-testimonials.liquid,
    sections/section-cta-banner.liquid,
    assets/component-hero.css,
    assets/component-curation-pillars.css,
    assets/component-category-browse.css,
    assets/component-featured-products.css,
    assets/component-brand-story.css,
    assets/component-how-it-works.css,
    assets/component-testimonials.css,
    assets/component-cta-banner.css,
    assets/component-product-card.css
  </files>
  <action>
    Implement full HTML/Liquid markup and CSS for all 8 sections. Follow the patterns established in Phase 1: CSS custom properties only (no hardcoded hex or pixel values where tokens exist), section-specific JS inline at the bottom of the liquid file inside `<script type="module">`.

    **section-hero.liquid** — Full implementation:
    - HTML: `<section class="section-hero">` wrapping a `.hero__content` column and a `.hero__media` column
    - Headline: `<h1 class="hero__headline">{{ section.settings.headline }}</h1>` — Newsreader serif via `font-family: var(--font-display)`
    - Subheadline: `<p class="hero__subheadline">{{ section.settings.subheadline }}</p>` — Inter via `font-family: var(--font-body)`
    - CTA buttons: primary uses `.btn.btn--primary` (red fill), secondary uses `.btn.btn--outline` (outline). Render conditionally when label is not blank.
    - Hero image: `{{ section.settings.hero_image | image_url: width: 1200 | image_tag: loading: 'eager', fetchpriority: 'high', ... }}` — CRITICAL: `loading="eager"` not lazy (HOME-02 requirement). Use `fetchpriority="high"` attribute.
    - Responsive: two-column layout on desktop (>=768px), stacked on mobile

    **component-hero.css**:
    - `.section-hero`: `display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-12); padding: var(--section-gap) var(--space-6)` at desktop; single column stacked on mobile
    - `.hero__headline`: `font-family: var(--font-display); font-size: clamp(2.5rem, 5vw, 4.5rem); font-weight: 700; line-height: 1.1`
    - `.hero__subheadline`: `font-family: var(--font-body); font-size: 1.125rem; color: var(--color-text-secondary); max-width: 40ch`
    - `.btn`: `display: inline-flex; align-items: center; padding: 0.875rem 2rem; border-radius: 100px; font-weight: 600; transition: var(--transition-base); cursor: pointer`
    - `.btn--primary`: `background: var(--color-brand-primary); color: #fff; border: none`
    - `.btn--outline`: `background: transparent; color: var(--color-brand-primary); border: 2px solid var(--color-brand-primary)`
    - `.hero__image`: `width: 100%; aspect-ratio: 4/3; object-fit: cover; border-radius: var(--radius-lg, 1.5rem)`

    **section-curation-pillars.liquid** — 3-block section:
    - `<section class="section-curation-pillars">` with `<h2>{{ section.settings.headline }}</h2>`
    - Block loop: `{%- for block in section.blocks -%}` rendering `.pillar-card` with Material Symbol icon (`<span class="material-symbols-outlined">{{ block.settings.icon }}</span>`), headline, body text
    - 3-column grid at desktop; single column on mobile
    - Note: Material Symbols font is loaded in theme.liquid from Phase 1 (Google Fonts). Render icon name text inside the span.

    **section-category-browse.liquid** — Staggered 4-card layout (CRITICAL design signature):
    - 4 portrait cards at 3:4 aspect ratio using `aspect-ratio: 3/4`
    - Staggered offset: even-indexed cards (2nd and 4th) get `margin-top: 3rem` — implement via CSS `:nth-child(even)` selector on `.category-card`
    - Each card: `<a href="{{ block.settings.link }}" class="category-card">` wrapping `.category-card__image-wrapper` (overflow hidden for scale clip) and `.category-card__label` overlay
    - Image hover: `.category-card__image { transition: transform var(--transition-base); } .category-card:hover .category-card__image { transform: scale(1.05); }`
    - 4-column grid at >=1024px, 2-column at >=768px, single column at mobile

    **section-featured-products.liquid** — Product grid from collection:
    - Source: `{%- assign featured_collection = section.settings.collection -%}`
    - `{%- for product in featured_collection.products limit: 4 -%}` — render `snippets/product-card.liquid`
    - `{% render 'product-card', product: product %}`
    - Grid: 4 columns uniform at desktop, 2 at tablet, 1 at mobile
    - No section JS needed — Quick Add JS handled in Plan 02-02 (shared via product card data attributes)
    - Fallback: if collection is blank, render `{{ 'product-1' | placeholder_svg_tag }}` x4

    **section-brand-story.liquid** — 2-col editorial:
    - Left col: `<h2>{{ section.settings.headline }}</h2>` + `{{ section.settings.body }}` (richtext rendered directly — no | t filter on richtext) + pull-quote `<blockquote>{{ section.settings.pull_quote }}</blockquote>`
    - Right col: 2x2 CSS grid of 4 images (`section.settings.image_1` through `image_4`), square aspect ratio `1/1`, rounded corners
    - On mobile: stacked single column

    **section-how-it-works.liquid** — 3-step flow:
    - `<section class="section-how-it-works">` with headline, then `<ol class="steps-list">` iterating blocks
    - Each step: `<li class="step-card">` with `.step-number` (gradient circle: `background: linear-gradient(135deg, var(--color-brand-primary), var(--color-brand-secondary, #ff6b6b)); border-radius: 50%`), `.step-headline`, `.step-body`
    - 3-column horizontal layout at desktop; single column stacked on mobile

    **section-testimonials.liquid** — Quote cards:
    - `<section class="section-testimonials">` with headline "Loved by Bob-scribers"
    - Block loop renders `.testimonial-card` with: star rating (render `rating` filled stars using star entity or SVG, 5 stars max), `<blockquote>`, customer name + location in `<cite>`
    - Avatar: render `{{ block.settings.avatar | image_url: width: 60 | image_tag: class: 'testimonial-avatar', loading: 'lazy' }}` if present
    - Layout: CSS `display: flex; flex-wrap: wrap; gap: var(--space-6)` — cards flow naturally; no JS carousel needed

    **section-cta-banner.liquid** — Full-width red rounded card:
    - `<section class="section-cta-banner">` — `.cta-banner__inner` with `background: var(--color-brand-primary); border-radius: 2rem; padding: var(--space-16) var(--space-8); color: #fff`
    - Headline: `<h2>{{ section.settings.headline }}</h2>` in Newsreader serif
    - Offer copy: `<p>{{ section.settings.offer_copy }}</p>`
    - CTA button: `<a href="{{ section.settings.cta_url }}" class="btn btn--white">{{ section.settings.cta_label }}</a>` — `.btn--white { background: #fff; color: var(--color-brand-primary) }` (white fill, red text — matching hero-gradient treatment from Figma)
    - Center-aligned text and button

    **component-product-card.css** — Shared styles used by both homepage featured grid and PLP:
    - `.product-card`: `display: flex; flex-direction: column; background: var(--color-surface); border-radius: var(--radius-md, 1rem); overflow: hidden; position: relative`
    - `.product-card__image-wrapper`: `aspect-ratio: 1/1; overflow: hidden`
    - `.product-card__image`: `width: 100%; height: 100%; object-fit: cover; transition: transform var(--transition-base)`
    - `.product-card:hover .product-card__image`: `transform: scale(1.05)`
    - `.product-card__badge`: `position: absolute; top: 0.75rem; left: 0.75rem; background: var(--color-surface); color: var(--color-text); font-size: 0.75rem; font-weight: 600; padding: 0.25rem 0.75rem; border-radius: 100px`
    - `.product-card__info`: `padding: 1rem`
    - `.product-card__title a`: `font-family: var(--font-body); font-weight: 600; color: var(--color-text); text-decoration: none`
    - `.product-card__price`: `color: var(--color-text-secondary); font-size: 0.9375rem`
    - `.product-card__quick-add`: `margin: 0 1rem 1rem; padding: 0.75rem; width: calc(100% - 2rem); background: var(--color-brand-primary); color: #fff; border: none; border-radius: var(--radius-sm, 0.5rem); font-weight: 600; cursor: pointer; transition: var(--transition-base)` — no JS here

    After implementing all sections and CSS, run `shopify theme check` and fix any errors before committing.
  </action>
  <verify>
    <automated>shopify theme check</automated>
  </verify>
  <done>shopify theme check passes zero errors and zero warnings. All 8 section liquid files have complete HTML/Liquid markup and full Theme Editor schemas. All 8 CSS files have complete styles using CSS custom properties. snippets/product-card.liquid is complete with image, badge, title, price, and Quick Add button.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify homepage sections match Figma and Theme Editor works</name>
  <action>Human verification step — see what-built and how-to-verify below.</action>
  <what-built>
    All 8 homepage sections implemented and wired into templates/index.json. Sections are: Hero (display headline + hero image + CTA buttons), Curation Pillars (3 icon blocks), Category Browse (4 staggered portrait cards), Featured Products (4-column product grid from collection), Brand Story (2-col editorial + 2x2 image grid), How It Works (3-step gradient circles), Testimonials (star-rated quote cards), CTA Banner (full-width red rounded card). Shared snippets/product-card.liquid created for use by both homepage and PLP.
  </what-built>
  <how-to-verify>
    1. Run `shopify theme dev` to start the development server
    2. Open the dev store preview URL in Chrome at 1280px viewport width
    3. Verify the homepage renders all 8 sections in order from top to bottom, with no blank sections or Liquid errors
    4. Compare side-by-side with the barterbobs_main_landing_page Figma export: verify section order, Newsreader serif on headlines, Inter on body text, red brand color on CTAs
    5. Check the hero image loads with `loading="eager"` — inspect the hero `<img>` tag in DevTools and confirm `loading="eager"` and `fetchpriority="high"` attributes are present
    6. Check Category Browse section: confirm alternate (2nd and 4th) cards are offset down ~3rem relative to the 1st and 3rd cards (staggered asymmetry from Figma)
    7. Open Shopify Theme Editor (Customize) — verify all 8 sections appear in the sidebar, add/edit a testimonial block, change the hero headline, and confirm edits reflect in the preview
    8. Resize viewport to 375px — verify sections stack to single column with no overflow or horizontal scroll
  </how-to-verify>
  <verify>
    <automated>shopify theme check</automated>
  </verify>
  <done>User confirms homepage sections visually match Figma export, hero image has loading="eager", staggered category cards are asymmetric, and Theme Editor section editing works.</done>
  <resume-signal>Type "approved" if sections match Figma and Theme Editor is functional, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `shopify theme check` passes zero errors and zero warnings after Task 1 (stubs) and Task 2 (full implementation)
- templates/index.json references all 8 section types in the correct Figma order
- Hero image tag has `loading="eager"` and `fetchpriority="high"` attributes (HOME-02)
- section-featured-products.liquid uses `{% render 'product-card', product: product %}` (not inline card markup)
- All section schemas have correct block type names matching index.json block type keys
- All CSS files use CSS custom properties (no hardcoded hex colors except #fff for white-on-red contrast where token unavailable)
</verification>

<success_criteria>
1. Homepage renders all 8 sections in the barterbobs_main_landing_page Figma order
2. Hero image has loading="eager" and fetchpriority="high" — no LCP penalty
3. All 8 sections are fully configurable via Shopify Theme Editor section schema blocks
4. Featured Products section pulls from merchant-selected collection via section.settings.collection
5. Category Browse has staggered offset (alternate cards offset ~3rem down) preserving Figma design signature
6. shopify theme check passes zero errors and zero warnings
7. snippets/product-card.liquid is complete and shared — ready for PLP in Plan 02-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-plp/02-01-SUMMARY.md`
</output>
